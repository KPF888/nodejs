session：
1.将加密的session加密并储存在服务器，每一个浏览器窗口只有一个对应的session，为浏览器的唯一标识符
2.手动判断session对象中的自定义数据，比如是否登录，如果登录了，那么就可以返回登陆后的页面

token:
1.将加密的用户信息（一般不包含密码，因为有被解密的风险）序列化为token，并发送给浏览器，sign浏览器
2.注册全局中间件，标记哪些地址需要token才能显示